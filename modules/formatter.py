"""
Data formatting module for query results
"""


def format_dataframe(df):
    """
    Return dataframe as-is (no formatting applied)
    
    Args:
        df (pandas.DataFrame): DataFrame from query
        
    Returns:
        pandas.DataFrame: Same DataFrame
    """
    # No formatting applied - return as-is
    return df
